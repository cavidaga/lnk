{
  "name": "vercel-media-analyzer-final",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "node -e \"require('@puppeteer/browsers').install_browser({ browser: 'chrome-headless-shell', buildId: '127.0.0.0', cacheDir: require('path').join(__dirname, '.cache')})\""
  },
  "dependencies": {
    "@puppeteer/browsers": "^1.1.1",
    "puppeteer": "^22.13.1"
  }
}
